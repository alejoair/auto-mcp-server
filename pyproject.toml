[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auto-mcp-server"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.71",
]
